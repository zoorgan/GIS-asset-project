# Server
PORT=4000
NODE_ENV=development

# PostgreSQL / PostGIS
PGHOST=localhost
PGPORT=5432
PGDATABASE=gis_assets
PGUSER=postgres
PGPASSWORD=postgres
PG_POOL_MAX=20

# JWT
JWT_SECRET=change_this_to_a_long_random_secret_in_production
JWT_EXPIRES_IN=8h

# CORS
CORS_ORIGIN=http://localhost:4200
