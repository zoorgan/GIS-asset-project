name: Comprehensive Code Evaluation

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  evaluate-frontend:
    name: Evaluate Frontend (Angular)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: gis-asset-frontend/package-lock.json

      - name: Install Chromium for Karma Tests
        run: |
          sudo apt-get update
          sudo apt-get install -y chromium-browser

      - name: Install Dependencies
        run: |
          cd gis-asset-frontend
          npm ci

      - name: Run Tests & Build Check
        env:
          CHROME_BIN: /usr/bin/chromium-browser
        run: |
          cd gis-asset-frontend
          npx ng test --watch=false --browsers=ChromeHeadlessNoSandbox
          npx ng build --configuration=production

  evaluate-backend:
    name: Evaluate Backend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install & Run Tests
        run: |
          cd gis-asset-backend
          npm ci
          npm test --if-present